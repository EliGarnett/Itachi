package priv.eli.Itachi.demo;

import com.alibaba.fastjson.JSON;
import priv.eli.Itachi.entity.DemoEntity;

import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.Stream;

/**
 * @author Eli
 * @date 2020/10/10
 */
public class StreamDemo {

    public static void main(String[] args) {

        //        Integer num = 1;
        //
        //        List<DemoEntity> entityList = Stream.of(num).map(String::valueOf).map(DemoEntity::new).collect(Collectors.toList());
        //        System.out.println(JSON.toJSONString(entityList));

    }
}
